    # Node.js
    node_modules/
    backend/.env  # Remember to create your own .env file after cloning the repo
    frontend/.env # Remember to create your own .env file after cloning the repo
    .env

    # React
    build/
    .env.local
    .env.development.local
    .env.test.local
    .env.production.local
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*
    .pnp
    .pnp.js

    # macOS
    .DS_Store

    # Windows
    Thumbs.db